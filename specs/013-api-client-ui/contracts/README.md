# UI API Contracts

This UI client is a pure consumer of the `appointment-api-service` REST endpoints. It does not expose any public endpoints or programmatic interfaces to other applications.

For the backend API contracts it consumes, please see the `appointment-api-service` Swagger/OpenAPI documentation.
